apiVersion: v1
kind: Service
metadata:
  name: weeb
  namespace: argocd
spec:
  type: NodePort
  externalTrafficPolicy: Cluster
  internalTrafficPolicy: Cluster
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: weeb
